# Copy to `.env` in this folder and fill in. Never commit `.env`.
SUPABASE_URL=
SUPABASE_ANON_KEY=
# Required for admin/profile endpoints that need service role (not the anon key).
SUPABASE_SERVICE_ROLE_KEY=
RECAPTCHA_SECRET_KEY=
